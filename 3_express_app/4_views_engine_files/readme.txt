topic covered are
1. create various view files (ejs file) and rendered them
2. how titled dynamically in views engine files
3.what is locals in  views
varible inside views engine are locals(<%=variable_name%>  or <%=locals.title%> both works fine)